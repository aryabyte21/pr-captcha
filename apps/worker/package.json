{
  "name": "@pr-captcha/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply pr-captcha --local"
  },
  "dependencies": {
    "hono": "^4.8.4",
    "jose": "^6.0.11",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250614.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.20.5"
  }
}
