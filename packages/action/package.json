{
  "name": "@pr-captcha/action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node20 --out-dir dist --clean",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "tsup": "^8.5.0"
  }
}
