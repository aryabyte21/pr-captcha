name: pr-captcha gate
description: Fail fast until pr-captcha has verified the exact pull request head SHA.
author: pr-captcha
inputs:
  api-url:
    description: Base URL for the pr-captcha Worker.
    required: true
runs:
  using: node20
  main: dist/index.js
